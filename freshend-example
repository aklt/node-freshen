# Example of a ~/.freshend config file

# Read .freshenrc in each of these directories and start an instance of
# freshen accordingly.  If no .freshenrc file is present throw an error.
dirs: [
  'node-freshen/example'
  'node-macroffee'
  # 'vim-substitute'
].map (dirname) ->
   return "#{process.env.HOME}/projects/#{dirname}"

# Where to access the freshend server
url: 'http://localhost:5000'
